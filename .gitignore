# Secrets - never commit real values. Copy .env.example to .env locally.
.env
.env.*.local
*.env

# Generated runtime artifacts (invoice PDFs) - these were previously
# committed to the repo (backend/generated/invoices/*.pdf); prevented going
# forward. Removing the already-committed ones is a separate, deliberate
# git-history decision, not done automatically here.
backend/generated/

# OS
.DS_Store
Thumbs.db

# Docker
docker-compose.override.yml
